# Optional SOCKS5 proxy for routing Steam connections
# PROXY_URL=******host:port

# Optional: delay in milliseconds between testing each account (default: 2000)
# DELAY_BETWEEN_ACCOUNTS=2000

# CS2 server to join when using --join-server mode (defaults shown)
# SERVER_IP=127.0.0.1
# SERVER_PORT=27015
