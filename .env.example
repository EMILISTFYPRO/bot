# Steam bot credentials (one per account, or use the database)
# Add accounts to the database via: node dbManager.js

# Optional: SOCKS5 proxy URL for Steam connections
# PROXY_URL=******host:1080

# Optional: override default delay between account checks (ms)
# DELAY_BETWEEN_ACCOUNTS=2000
